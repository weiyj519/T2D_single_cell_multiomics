# Cell Communication

This module contains MultiNicheNet ligand-receptor analysis and plotting scripts. Cell type and comparison choices are configured through `configs/config.yaml` and script-level parameters.

