# Data

Raw data and processed data are not included in this repository.

Users should download or prepare input data independently, then update `configs/config.yaml` with local paths for raw data, processed feature matrices, Seurat objects, metadata, and external reference resources.

