# Examples

The example files in this directory demonstrate expected STAGE gene and STAGE peak table formats only.

Full results are not included and should be regenerated by running the analysis scripts with configured data paths.

